// import * as types from './types'

const marketState = {
  updated: false
}

const marketReducer = (state = marketState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default marketReducer
